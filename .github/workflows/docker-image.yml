name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:

  push_to_registry:
    name: Push Docker images to Docker Hub
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out the repo 
      uses: actions/checkout@v3
        
    - name: Log in to Docker Hub
      uses: docker/login-action@f054a8b539a109f9f41c372932f1ae047eff08c9
      with:
        username: ${{secrets.DOCKER_USERNAME}}
        password: ${{secrets.DOCKER_PASSWORD}}
        
    - name: Extract metadata (tags, labels) for Docker
      id: meta
      uses: docker/metadata-action@98669ae865ea3cffbcbaa878cf57c20bbf1c6c38
      with:
        images: wesje101/s6-2_personal_project
        
    - name: Build and publish Docker image
      uses: docker/build-push-action@ad44023a93711e3deb337508980b4b5e9bcdc5dc
      with:
        context: ./RabbitMQPrototype/AccountService
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.output.labels }}
        
    #- uses: actions/checkout@v3
    #- name: Build account service image
    #  working-directory: ./RabbitMQPrototype/AccountService 
    #  run: docker build . --file Dockerfile --tag wesje101/s6-2_personal_project:accountservice
    #
    #- uses: actions/checkout@v3
    #- name: push account service image
    #  working-directory: ./RabbitMQPrototype/AccountService
    #  run: docker push wesje101/s6-2_personal_project:accountservice
